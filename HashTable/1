#include<iostream>
#include"hashtable.h"
#include<unordered_set>
using namespace std;

int main(){
unordered_set<int>s;
cout<<s.size()<<endl;
cout<<s.insert(78).second<<endl;
cout<<s.insert(78).second<<endl;
for(int i=0;i<10;i++){
    s.insert(i);
}
for(unordered_set<int>::iterator it=s.begin();it!=s.end();it++){
    cout<<*it<<" ";
}
cout<<endl;
return 0;
}

